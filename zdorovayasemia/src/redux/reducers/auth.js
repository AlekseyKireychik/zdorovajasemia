const initialState  = {
    profile: { value: 'fixed'},
    toke: null,
    status: 'auth', // guest // in_progress // auth
};

const reducer = (state = initialState, action) => {
    return state;
}

export default reducer;
